apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Release.Namespace }}
  labels:
    hks.hegerdes.com/managed-cluster: "true"
    hks.hegerdes.com/region: "{{ .Values.controlplane.domainSuffix }}"

    {{- with .Values.namespaceLabels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec: {}
